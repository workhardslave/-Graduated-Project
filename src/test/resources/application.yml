spring:
  datasource:
    url: jdbc:h2:mem:guhaedog
    username: sa
    password: 1234
    driver-class-name: org.h2.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
